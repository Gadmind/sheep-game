# 🔧 移出道具功能详解

## 功能说明

v2.0.3.3 改进了"移出"道具的功能，现在使用移出道具时，卡片会被放入独立的"移出区域"，而不是直接消除。

---

## ✨ 新功能

### Before（旧版本）
```
使用移出道具：
  卡槽：[🐑, 🌟, ⭐, 🌙, ☀️]
     ↓
  直接删除前3张
     ↓
  卡槽：[🌙, ☀️]

问题：
✗ 卡片永久消失
✗ 无法恢复（除了撤销）
✗ 可能误操作
```

### After（新版本）
```
使用移出道具：
  卡槽：[🐑, 🌟, ⭐, 🌙, ☀️]
     ↓
  移到移出区
     ↓
  卡槽：[🌙, ☀️]
  移出区：[🐑, 🌟, ⭐]

优势：
✓ 卡片暂存在移出区
✓ 可以手动清空
✓ 可以看到移出的卡片
✓ 防止误操作
```

---

## 🎨 UI展示

### 移出区域
```
┌─────────────────────────────┐
│ 移出区 0/3        [🗑️清空]  │
├─────────────────────────────┤
│  [空]                       │
│  （红色背景，等待卡片）       │
└─────────────────────────────┘
```

### 使用移出道具后
```
┌─────────────────────────────┐
│ 移出区 3/3        [🗑️清空]  │
├─────────────────────────────┤
│  🐑  🌟  ⭐                 │
│  （显示被移出的3张卡片）     │
└─────────────────────────────┘
```

---

## 🎮 使用流程

### 步骤1：卡槽有卡片
```
卡槽：[🐑, 🌟, ⭐, 🌙, ☀️, ☁️, ❄️]  ← 快满了
移出区：[空]
```

### 步骤2：使用移出道具
```
点击"移出"道具按钮
  ↓
卡槽：[🌙, ☀️, ☁️, ❄️]  ← 移出了前3张
移出区：[🐑, 🌟, ⭐]  ← 放入移出区
```

### 步骤3：继续游戏
```
卡槽：[🌙, ☀️, ☁️, ❄️]
移出区：[🐑, 🌟, ⭐]  ← 暂存在这里
```

### 步骤4：清空移出区（可选）
```
点击移出区的"清空"按钮
  ↓
移出区：[空]  ← 卡片被永久删除
```

---

## 🔧 功能特性

### 1. 自动管理
- ✅ 最多存放3张卡片
- ✅ 满了会提示
- ✅ 实时计数显示

### 2. 手动控制
- ✅ 清空按钮
- ✅ 主动清理
- ✅ 灵活管理

### 3. 可视化
- ✅ 独立显示区域
- ✅ 红色主题区分
- ✅ 卡片样式不同

### 4. 可撤销
- ✅ 支持撤销移出操作
- ✅ 支持撤销清空操作
- ✅ 完整的历史记录

---

## 🎯 策略应用

### 使用场景

#### 场景1：卡槽快满时
```
卡槽：7张不同的卡
策略：使用移出道具，移出3张
效果：腾出空间，继续游戏
```

#### 场景2：等待特定卡片
```
卡槽：[🐑, 🌟, ⭐, ...]
需要：等2张🐑形成三消
策略：先移出其他卡片
效果：等到🐑后再清空移出区
```

#### 场景3：临时存储
```
移出区：[🌟, 🌟, ⭐]
发现：这些卡片可能有用
策略：暂时不清空
效果：保留选项，灵活应对
```

---

## 🔄 与其他功能的交互

### 1. 撤销功能
```
操作：使用移出道具
结果：3张卡到移出区
  ↓
操作：使用撤销道具
结果：卡片回到卡槽，移出区清空
```

### 2. 移出区满时
```
移出区：[🐑, 🌟, ⭐]  ← 已满3张
操作：再次使用移出道具
结果：提示"移出区已满，请先清空"
```

### 3. 清空移出区
```
移出区：[🐑, 🌟, ⭐]
操作：点击清空按钮
确认：卡片永久删除
  ↓
操作：使用撤销道具（如果有）
结果：卡片恢复到移出区
```

---

## 💡 设计理念

### 为什么改成移出区？

#### 1. 给玩家更多控制权
- ❌ 旧版：直接删除，不可控
- ✅ 新版：暂存，可手动清理

#### 2. 防止误操作
- ❌ 旧版：误点后卡片永久消失
- ✅ 新版：可以看到移出的卡片

#### 3. 增加策略性
- ❌ 旧版：简单的移除
- ✅ 新版：需要决定何时清空

#### 4. 视觉反馈
- ❌ 旧版：卡片消失，不知去哪
- ✅ 新版：清楚显示在移出区

---

## 🎨 样式设计

### 颜色区分

| 区域 | 背景色 | 边框色 | 卡片边框 |
|------|--------|--------|---------|
| 卡槽 | 灰白 #F8F9FA | 灰色 | 白色 |
| 移出区 | 淡红 #FFF5F5 | 红色 | 红色 |

### 视觉层次
```
卡槽（主要操作区）
  ├─ 蓝灰色调
  ├─ 标准边框
  └─ 白色卡片边框

移出区（临时存储区）
  ├─ 红色调 ← 警示作用
  ├─ 虚线边框
  └─ 红色卡片边框
```

---

## 📊 状态管理

### 数据结构

```javascript
state: {
    selectedCards: [],    // 卡槽中的卡片
    removedCards: [],     // 移出区的卡片（最多3张）
    // ...
}
```

### 操作历史

```javascript
moves: [
    {
        type: 'remove',
        cards: [card1, card2, card3],
        previousRemoved: []
    },
    {
        type: 'clear-removed',
        cards: [card1, card2, card3]
    }
]
```

---

## 🧪 测试用例

### 测试1：基本功能
```
操作：
1. 卡槽有5张卡片
2. 使用移出道具
3. 查看移出区

预期：
- 卡槽剩2张 ✓
- 移出区有3张 ✓
- 计数显示3/3 ✓
```

### 测试2：清空功能
```
操作：
1. 移出区有3张卡片
2. 点击清空按钮
3. 查看移出区

预期：
- 移出区变空 ✓
- 计数显示0/3 ✓
- 清空按钮禁用 ✓
```

### 测试3：撤销功能
```
操作：
1. 使用移出道具
2. 使用撤销道具

预期：
- 卡片回到卡槽 ✓
- 移出区清空 ✓
- 撤销次数-1 ✓
```

### 测试4：移出区满
```
操作：
1. 移出区已有3张
2. 再次使用移出道具

预期：
- 提示"移出区已满" ✓
- 道具次数不减少 ✓
- 没有变化 ✓
```

---

## 🔮 未来扩展

### 可能的改进
- [ ] 移出区容量可配置
- [ ] 从移出区恢复卡片到卡槽
- [ ] 移出区卡片可重新排序
- [ ] 移出区卡片可单独删除

---

## 📝 代码示例

### 使用移出道具
```javascript
useRemoveTool() {
    // 检查条件
    if (removedCards.length >= 3) {
        showMessage('移出区已满');
        return;
    }
    
    // 移出卡片
    const cards = selectedCards.splice(0, 3);
    removedCards.push(...cards);
    
    // 渲染
    renderRemovedSlot();
}
```

### 清空移出区
```javascript
clearRemovedCards() {
    // 记录历史
    moves.push({ type: 'clear-removed', cards });
    
    // 清空
    removedCards = [];
    
    // 渲染
    renderRemovedSlot();
}
```

---

## 🎊 总结

### 核心改进
- ✨ 卡片暂存而非直接删除
- ✨ 独立的移出区域显示
- ✨ 手动清空控制
- ✨ 完整的撤销支持

### 用户价值
- 更好的控制权
- 更清晰的反馈
- 防止误操作
- 增强策略性

---

**功能版本：** v2.0.3.3  
**更新日期：** 2026-01-28

---

*让道具使用更灵活，更可控！* 🔧
